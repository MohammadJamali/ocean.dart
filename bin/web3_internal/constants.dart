const String ZERO_ADDRESS = "0x0000000000000000000000000000000000000000";

BigInt MAX_UINT256 = BigInt.from(2).pow(256) - BigInt.one;

BigInt MAX_INT256 = BigInt.from(2).pow(255) - BigInt.one;
BigInt MIN_INT256 = BigInt.from(2).pow(255) * BigInt.from(-1);
